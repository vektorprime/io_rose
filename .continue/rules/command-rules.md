# How commands should be run

Only use Windows commands, Linux commands are NOT available.
Command Prompt (cmd) is the default terminal.

If you want to run Powershell commands, use this format
powershell -Command "<COMMAND HERE>"
e.g.
powershell -Command "Get-Process"

